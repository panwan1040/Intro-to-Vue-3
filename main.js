const app = Vue.createApp({
  data() {
    return {
      product: "Socks",
    };
  },
});

const des = Vue.createApp({
  data() {
    return {
      description: "ถุงเท้าเน่าเหม็น",
    };
  },
});
